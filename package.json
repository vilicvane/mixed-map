{
  "name": "mixed-map",
  "version": "0.1.2",
  "description": "A simple map implementation that prefers `WeakMap` but uses `Map` as backup for primitive keys.",
  "scripts": {
    "build": "tsc",
    "lint": "tslint --project tsconfig.json --type-check"
  },
  "main": "bld/index.js",
  "types": "bld/index.d.ts",
  "repository": "https://github.com/vilic/mixed-map.git",
  "author": "vilicvane",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^5.6.0",
    "typescript": "^2.5.2",
    "vts": "^5.6.2"
  }
}
